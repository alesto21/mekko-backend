-r requirements.txt

# Test-only dependencies. Not installed in the production Railway
# container (which only installs requirements.txt), so these never affect
# what actually runs in production.
pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.23.1
